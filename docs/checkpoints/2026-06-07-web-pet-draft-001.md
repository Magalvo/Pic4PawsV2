# Checkpoint: WEB-PET-DRAFT-001

Date: 2026-06-07
Branch: `codex/WEB-PET-DRAFT-001`
Base: `main` commit `5f0403c`

## Summary

Implemented the Web product boundary for creating and updating pet drafts through the shared safe pet draft client.

## Completed

- Added `docs/work-items/WEB-PET-DRAFT-001-web-pet-draft-product-flow.md`.
- Added `docs/work-specs/WEB-PET-DRAFT-001-web-pet-draft-product-flow.md`.
- Added `apps/web/src/pet-draft.ts`.
- Added `createWebPetDraftUi` with injected `draftClient.createPetDraft` and `draftClient.updatePetDraft`.
- The Web boundary sanitizes draft payloads before forwarding them to injected dependencies.
- Create and update successes return safe PT-PT copy plus `petId`, `petName` and operation metadata.
- Known draft failures map to distinct safe PT-PT Web product states.
- Failure reasons are sanitized to avoid bearer token, Supabase service-role or R2 credential leakage.
- Web foundation content now surfaces `petDraft`.
- Added tests in `tests/web/pet-draft-ui.test.ts`.
- Updated `docs/work-tracks/remake-foundation.md` and `docs/codex-resume.md`.

## Validation

- Targeted Vitest run passed for Web pet draft UI and Web foundation content.
- `npm run typecheck` passed.
- `npm run lint` passed.
- `npm run test` passed.
- `npm run build` passed.

## Current State

The Worker owns draft persistence validation, the shared client safely invokes the create/update routes, and the Web product boundary now exposes safe PT-PT draft editing states without forwarding client-only claims or credential-like fields.

## Recommended Next Work Item

Recommended next item: `MOBILE-PET-DRAFT-001`.

Goal: wire the shared pet draft client into the Mobile product boundary/view model with fake/injected dependencies first, mirroring the Web draft flow while keeping persistence validation on the Worker.
