---
audited-through: ff60670c871194c4b2df7eedf4736cbed7128aa1
prs: "#197–#198"
score: 9/10
---

# SDD Audit — PRs #197–#198 (2026-06-20)

**Auditor**: Independent SDD audit session
**Baseline**: `f2455105251acee682407cc28b0b60e582e6826a` (prior audit `2026-06-20-sdd-audit-main-nav-auth-reaudit.md`)
**Scope**: PRs #197 (`MOBILE-AUTH-P1-001`) and #198 (`WEB-MIDDLEWARE-P1-001`) plus non-merge commit `ff60670`
**Gates**: typecheck 15/15 ✅ · lint 8/8 + SDD hygiene ✅ · test 1926/1926 (241 files) ✅ · build 9/9 ✅

---

## 1. Spec Compliance Score: 9/10

Both P1 security/correctness findings from the prior audit are fully remediated with
code changes and tests. All three P2 findings are resolved. Work item metadata is
accurate. All four validation gates pass clean.

One point deducted for two residual gaps that were not present before this work track
and remain unaddressed after it:

- The mobile routing integration test (unauthenticated → redirect → sign-in → returnTo)
  is deferred and documented but still genuinely absent. It is the most important
  behavioral proof for the auth guard and the fix just merged.
- Two public routes (`/abrigos` and `/abrigos/[shelterId]`) present in the web
  middleware's `isPublicRoute` function are not covered by any test in
  `tests/web/middleware.test.ts`, leaving a partial gap in the middleware contract tests
  that were added as the P1-2 remediation.

---

## 2. Gap Analysis Matrix

| Feature / Requirement | Status | File Paths | Notes |
|---|---|---|---|
| P1-1: Shared mobile Supabase client (singleton) | Implemented | `apps/mobile/src/supabase.ts`, `apps/mobile/app/_layout.tsx`, `apps/mobile/app/(auth)/entrar.tsx` | `mobileSupabaseClient` singleton exported; both files import it; old inline `createClient` calls removed |
| P1-1: Auth-state propagation test | Implemented | `tests/mobile/auth-screen.test.ts` | New test proves `onAuthStateChange` subscriber on shared client fires when sign-in happens through same instance |
| P1-2: Middleware uses `getUser()` | Implemented | `apps/web/middleware.ts:38` | `supabase.auth.getUser()` replaces `getSession()`; guards on `user` not `session`; server-validates JWT |
| P1-2: Middleware tests | Implemented | `tests/web/middleware.test.ts` | 7 tests: unauthenticated redirect, 2 public route pass-throughs, auth `/entrar` default redirect, `next` param redirect, open-redirect rejection, authenticated protected pass-through |
| P2-1: WEB-NAV-001 status + completion notes | Implemented | `docs/work-items/WEB-NAV-001-web-navigation.md` | `status: done`, completion notes document the `getSession` bug, fix, and tests added |
| P2-2: MOBILE-NAV-001 completion notes + test reference | Implemented | `docs/work-items/MOBILE-NAV-001-mobile-navigation.md` | Completion notes document the two-client bug, singleton fix, propagation test, and deferred integration test with justification |
| P2-3: Graphify update | Implemented | `graphify-out/` (gitignored) | Updated per completion notes in MOBILE-NAV-001; confirmed in memory by dev session |
| `noImplicitAny` on `setAll` callback | Implemented | `apps/web/middleware.ts:26` | `CookieOptions` type imported from `@supabase/ssr` and applied to `setAll` parameter |
| Web auth-page redirect contract test | Implemented | `tests/web/auth-page.test.ts` | `signed_in result carries the accessToken` test proves the condition the page uses for `window.location.href` redirect |
| Mobile routing integration test (full flow) | Missing (deferred) | `tests/mobile/` | Unauthenticated → redirect → sign-in → returnTo flow not exercised; requires React Native Testing Library; explicitly deferred in MOBILE-NAV-001 completion notes |
| Middleware tests: `/abrigos` public routes | Partial | `tests/web/middleware.test.ts` | Tests cover `/animais` and `/animais/[petId]` pass-through but not `/abrigos` or `/abrigos/[shelterId]`, which are also listed in `isPublicRoute` |

---

## 3. Deviations and Scope Creep

None found. Both PRs are surgical fixes directly addressing prior audit findings. No
unrequested features, no new worker routes, no schema changes.

---

## 4. Actionable Next Steps

### P1 — None

Both prior P1 findings are closed.

### P2-A: Add `/abrigos` pass-through tests to middleware test suite

**File**: `tests/web/middleware.test.ts`
**Precedent**: existing tests at lines 37–47 for `/animais` and `/animais/pet-001`

Add two more `it` blocks:

```
it('unauthenticated request to /abrigos passes through', ...)
it('unauthenticated request to /abrigos/[shelterId] passes through', ...)
```

These mirror the `/animais` pass-through tests already present. Without them, a future
edit to `isPublicRoute` that accidentally removes the `/abrigos` clause would not be
caught by the test suite.

### P2-B: Mobile routing integration test (deferred, revisit when RNTI is set up)

**File**: `tests/mobile/routing-integration.test.ts` (new)
**Deferred reason**: requires React Native Testing Library to render `RootLayout` with
mocked `expo-router` hooks

When React Native Testing Library is available, add a test that:
1. Renders `RootLayout` with `mobileSupabaseClient` stubbed to return `session: null`
2. Navigates to a protected route (e.g. `/(app)/(tabs)/adocoes`)
3. Asserts redirect to `/(auth)/entrar?returnTo=...`
4. Stubs `mobileSupabaseClient` to fire `SIGNED_IN` with a live session
5. Asserts final navigation lands on the `returnTo` path without a second redirect

This is the only outstanding gap from the navigation work track.

### P3: Update `docs/agent-resume.md` checkpoint

The last checkpoint (`2026-06-13-shelter-update-atomic-complete.md`) is stale — it
reflects the project state at PR #136. A new checkpoint should be written covering:
- Navigation (PRs #195–#196), auth loop fix (#197), middleware fix (#198)
- Test count: 1926 passing / 241 files
- All work items: `done`

This is housekeeping, not a correctness issue.
